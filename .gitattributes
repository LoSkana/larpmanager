*.sql filter=lfs diff=lfs merge=lfs -text
# Exclude test_db.sql from LFS - now uses normalized dates for stability
larpmanager/tests/test_db.sql -filter -diff -merge text
# Binary reset snapshot for fast per-test pg_restore
larpmanager/tests/test_db.dump filter=lfs diff=lfs merge=lfs -text
